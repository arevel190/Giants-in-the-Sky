enum TuioAction
{
    ADD,
    REMOVE,
    UPDATE
    
};

enum StellarObject // Here is where we create the types of stars or celestial bodies to pair them with a fiducial value
{
  YELLOW,
  RED_DWARF,
  BLUE,
  BLACK_HOLE,
  LEARN_SCREEN,
}